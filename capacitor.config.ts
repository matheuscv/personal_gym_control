import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.matheuscv.personalgymcontrol',
  appName: 'Personal Gym Control',
  webDir: 'dist'
};

export default config;
